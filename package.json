{
  "name": "claude-code-cockpit",
  "version": "0.9.0",
  "private": true,
  "description": "Live, cross-platform dashboard of all running Claude Code sessions — status, timers, and per-repo token/time/cost accounting, with optional OS notifications and sounds.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test scripts/*.test.js"
  },
  "dependencies": {
    "node-notifier": "^10"
  }
}
