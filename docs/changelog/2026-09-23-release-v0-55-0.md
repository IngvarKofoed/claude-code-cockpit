# Released v0.55.0

Released v0.55.0, carrying the on-screen group picker and delete-from-picker
(`2026-09-23-group-picker-placement-and-delete`). Bumped per entry 103 — the plugin cache is
keyed by version, so a UI-only change still needs the bump to reach a fresh install.
`marketplace.json` stays at 0.2.0, unmaintained, so it is left alone.
